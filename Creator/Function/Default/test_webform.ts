void test_webform(string fisrt_name, string last_name)
{
	lead_map = Map();
	if(fisrt_name.isEmpty() != true && last_name.isEmpty() != true)
	{
		lead_map.put("Choose_Primary_Contact","Créer nouveau contact/Create new contact");
	}
	info lead_map;
}